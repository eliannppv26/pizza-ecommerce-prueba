'use strict';

/**
 * store-configuration router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::store-configuration.store-configuration');
