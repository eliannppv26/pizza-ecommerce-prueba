// El filtro por "origen" del template de café no aplica a las pizzas.
// El filtrado ahora vive en filters-controls-category.tsx (por etiqueta).
export default function FilterOrigin() {
  return null
}
